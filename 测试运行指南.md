# 测试运行指南

## 快速测试（使用SQLite，无需MySQL）

### 1. 后端启动

```bash
cd backend

# 安装Python依赖
pip install -r requirements.txt

# 启动测试服务器（使用SQLite）
python start_test.py
```

后端将在 http://localhost:8000 启动

### 2. 前端启动

打开新的终端窗口：

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端将在 http://localhost:3000 启动

## 测试步骤

### 1. 访问前端
打开浏览器访问：http://localhost:3000

### 2. 注册账号
- 点击"立即注册"
- 输入手机号和密码（至少6位）
- 完成注册

### 3. 上传图片分析
- 登录后点击"开始分析"
- 上传至少5张朋友圈截图
- （可选）填写补充信息
- 点击"开始分析"

**注意**：如果没有配置豆包API密钥，分析功能会失败。需要：
1. 从火山引擎获取API密钥
2. 在 `backend/test_config.py` 中配置 `DOUBAO_API_KEY`
3. 或者设置环境变量：`export DOUBAO_API_KEY=your-key`

### 4. 查看结果
- 分析完成后会自动跳转到结果页面
- 可以查看各个维度的分析结果
- 可以尝试多轮对话功能

### 5. 查看历史记录
- 点击底部导航栏的"历史"
- 查看之前的分析记录

## 配置说明

### 使用SQLite（测试用）
- 无需安装MySQL
- 数据库文件：`backend/test.db`
- 适合快速测试

### 使用MySQL（生产环境）
1. 安装MySQL
2. 创建数据库：`CREATE DATABASE friend_circle_analyzer;`
3. 修改 `backend/config.py` 中的 `DATABASE_URL`
4. 使用 `python -c "from database import init_db; init_db()"` 初始化
5. 使用 `uvicorn main:app --reload` 启动

## 常见问题

### 1. 端口被占用
- 后端：修改 `start_test.py` 中的端口号
- 前端：修改 `vite.config.js` 中的端口号

### 2. 依赖安装失败
- Python：确保使用Python 3.9+
- Node.js：确保使用Node.js 16+

### 3. API调用失败
- 检查 `DOUBAO_API_KEY` 是否配置
- 检查网络连接
- 查看后端日志错误信息

### 4. 数据库错误
- SQLite：确保有写入权限
- MySQL：检查连接配置和数据库是否存在

## 测试账号

可以创建测试账号：
- 手机号：任意11位数字（如：13800138000）
- 密码：至少6位（如：123456）

## 功能测试清单

- [ ] 用户注册
- [ ] 用户登录
- [ ] 图片上传（5-20张）
- [ ] AI分析（需要API密钥）
- [ ] 查看分析结果
- [ ] 多轮对话
- [ ] 历史记录
- [ ] 分享链接
- [ ] 修改密码

## 下一步

1. 配置真实的豆包API密钥
2. 配置云存储（可选）
3. 部署到生产环境
