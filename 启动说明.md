# 项目启动说明

## 环境要求

### 前端
- Node.js >= 16.0.0
- npm 或 yarn

### 后端
- Python >= 3.9
- MySQL >= 5.7

## 快速开始

### 1. 后端设置

```bash
cd backend

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
# 复制 config.py 并根据实际情况修改配置
# 需要配置：
# - DATABASE_URL: MySQL数据库连接
# - SECRET_KEY: JWT密钥
# - DOUBAO_API_KEY: 豆包API密钥
# - DOUBAO_API_URL: 豆包API地址
# - TOS相关配置（如果使用云存储）

# 初始化数据库
python -c "from database import init_db; init_db()"

# 启动服务
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 前端设置

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 3. 访问应用

- 前端地址：http://localhost:3000
- 后端API：http://localhost:8000
- API文档：http://localhost:8000/docs

## 配置说明

### 后端配置（backend/config.py）

```python
# 数据库配置
DATABASE_URL = "mysql+pymysql://user:password@localhost:3306/friend_circle_analyzer"

# JWT配置
SECRET_KEY = "your-secret-key-here"  # 请修改为随机字符串
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 1440

# 豆包API配置
DOUBAO_API_KEY = "your-doubao-api-key"  # 从火山引擎获取
DOUBAO_API_URL = "https://ark.cn-beijing.volces.com/api/v3"  # 根据实际区域修改
DOUBAO_MODEL = "doubao-seed-1.6-thinking"

# 云存储配置（可选，当前使用本地存储）
TOS_ACCESS_KEY = ""
TOS_SECRET_KEY = ""
TOS_ENDPOINT = ""
TOS_BUCKET = ""
```

### 前端配置（frontend/vite.config.js）

```javascript
// 如果后端地址不是 localhost:8000，需要修改 proxy 配置
proxy: {
  '/api': {
    target: 'http://localhost:8000',  // 修改为实际后端地址
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api/, '')
  }
}
```

## 功能说明

### 已实现功能

✅ 用户登录注册（手机号+密码）
✅ 图片上传（5-20张，单张10MB）
✅ 豆包大模型分析集成
✅ 分析结果展示（可折叠）
✅ 历史记录查看
✅ 多轮对话功能
✅ 分享链接生成
✅ 标签分类
✅ 修改密码

### 待完善功能

- 图片导出功能
- 批量导出历史记录
- 分享链接功能
- 云存储集成（当前使用本地存储）

## 注意事项

1. **数据库初始化**：首次运行需要执行数据库初始化命令
2. **API密钥**：需要从火山引擎获取豆包API密钥
3. **文件存储**：当前使用本地存储，生产环境建议使用云存储
4. **安全性**：生产环境请修改SECRET_KEY为随机字符串

## 常见问题

### 1. 数据库连接失败
- 检查MySQL服务是否启动
- 检查DATABASE_URL配置是否正确
- 确保数据库已创建

### 2. API调用失败
- 检查DOUBAO_API_KEY是否正确
- 检查网络连接
- 查看后端日志错误信息

### 3. 前端无法连接后端
- 检查后端服务是否启动
- 检查vite.config.js中的proxy配置
- 检查CORS配置

## 开发建议

1. 使用虚拟环境管理Python依赖
2. 使用环境变量管理敏感配置
3. 定期备份数据库
4. 监控API调用成本
