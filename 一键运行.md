# 一键运行（给不想折腾的人）

## 方式1：测试模式（推荐，SQLite）

### 1）准备
- 确保已安装：Python 3.9+、Node.js 16+

### 2）一键启动

在项目根目录运行：

```bash
chmod +x scripts/start_test_mode.sh
./scripts/start_test_mode.sh
```

启动后：
- 前端：`http://localhost:3000`
- 后端：`http://localhost:8000`
- API 文档：`http://localhost:8000/docs`

> 若你要测试真实分析，请在 `backend/.env` 填写 `DOUBAO_API_KEY` 后重启。

---

## 方式2：正式模式（MySQL + 可选 TOS）

### 1）创建 MySQL 数据库

执行：`scripts/init_mysql.sql`

### 2）配置后端

复制并修改：
- `backend/env.template` → `backend/.env`
- 把 `DATABASE_URL` 改为 MySQL 连接串
- 填写 `DOUBAO_API_KEY`
- 如果用 TOS：填写 `TOS_*` 并设置 `STORAGE_BACKEND=tos`（还需要安装 TOS SDK）

### 3）启动后端

```bash
cd backend
pip install -r requirements.txt
python -c "from database import init_db; init_db()"
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 4）启动前端

```bash
cd frontend
npm install
npm run dev
```

