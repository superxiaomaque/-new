# 快速启动测试

## 最简单的方式

### 1. 启动后端（使用SQLite，无需MySQL）

```bash
cd backend
pip install -r requirements_test.txt
python test_main.py
```

后端将在 http://localhost:8000 启动

### 2. 启动前端

打开新终端：

```bash
cd frontend
npm install
npm run dev
```

前端将在 http://localhost:3000 启动

## 测试步骤

1. 访问 http://localhost:3000
2. 注册账号（任意11位手机号，密码至少6位）
3. 登录
4. 上传至少5张图片进行分析

## 注意事项

- 测试模式使用SQLite，无需安装MySQL
- 如果没有配置豆包API密钥，分析功能会失败
- 配置API密钥：设置环境变量 `export DOUBAO_API_KEY=your-key`
